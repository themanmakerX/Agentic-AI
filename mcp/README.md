# MCP

This directory contains the repository's MCP-related bundles.

- [`code-review-graph/`](code-review-graph/) - Python MCP package for code review graph analysis, graph queries, workspace audit, and TOML installer support
- [`vault/`](vault/) - JavaScript MCP plugin bundle for persistent agent memory, vault selection, records, facts, links, journals, and checkpoint hooks
