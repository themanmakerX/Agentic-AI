# MCP

This directory contains the repository's MCP-related packages and plugins.

- [`knowledge-graph/`](knowledge-graph/) - Python MCP package for persistent code graph indexing, graph queries, architecture exploration, workspace analysis, and web-based visualization.
- [`vault/`](vault/) - MCP plugin bundle for persistent agent memory, vault selection, records, facts, links, journals, hooks, and checkpoint workflows.
